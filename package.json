{
  "name": "devassist-ui",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n bridge,spa -c cyan,magenta \"npm run dev --workspace=packages/bridge\" \"npm run dev --workspace=packages/spa\"",
    "build": "npm run build --workspace=packages/spa",
    "build:extension": "npm run build --workspace=packages/extension",
    "dev:extension": "npm run watch --workspace=packages/extension",
    "test": "npm run test --workspace=packages/bridge && npm run test --workspace=packages/spa && npm run verify:sections",
    "test:bridge": "npm run test --workspace=packages/bridge",
    "test:spa": "npm run test --workspace=packages/spa",
    "bridge": "npm run build --workspace=packages/spa && npm run build --workspace=packages/bridge && npm run start --workspace=packages/bridge",
    "bridge:start": "npm run start --workspace=packages/bridge",
    "bridge:dev": "npm run dev --workspace=packages/bridge",
    "verify:sections": "node scripts/verify-sections-no-truncation.mjs"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "overrides": {
    "body-parser": "^1.20.3",
    "express": "^4.21.2",
    "qs": "^6.16.0"
  }
}
